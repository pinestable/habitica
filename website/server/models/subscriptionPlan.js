// Stubbed - subscription plan (multiplayer billing) removed
import mongoose from 'mongoose';

export const schema = new mongoose.Schema({});
export const model = mongoose.model('SubscriptionPlan', schema);
